pipeline {
     agent any
     stages {
          stage("run frontend") {
               steps {
		       echo 'executing yarn....'
                   // sh "./gradlew compileJava"
               }
          }
	     
	           stage("run backend") {
               steps {
		       echo 'executing gradle....'
                   // sh "./gradlew compileJava"
               }
          }
         
	
}
}
